<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
		<link rel="stylesheet" href="/stylesheets/loginpage.css" />
		<link rel="icon" type="image/x-icon" href="/images/myfavicon.ico" />
	</head>
	<body>
		<%- include('templates/navbar-home') %>
		<form action="" method="post">
			<div class="form-opt">
				<label for="username">Username</label>
				<input type="text" name="username" id="username" value="<%= (typeof username !== 'undefined') ? username : '' %>" required />
			</div>
			<div class="form-opt">
				<label for="password">Password</label>
				<div class="password-container">
					<input type="password" name="password" id="password" required />
					<img class="eye-icon" src="/images/eye.svg" alt="password eye-icon" />
				</div>
			</div>
			<% if (typeof error !== 'undefined') { %>
			<p class="error"><%= error %></p>
			<% } %>
			<button type="submit"><p>Log In</p></button>
		</form>
	</body>
	<script src="/javascripts/toggle_password.js"></script>
</html>
